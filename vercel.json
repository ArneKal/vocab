{
    "version": 2,
    "builds": [{ "src": "public/index.gtml", "use": "@vercel/node" }],
    "routes": [
      {
        "src": "/(.*)",
        "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
      }
    ]
  }